{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Username=postgres;Database=CardOverflow;",
    "TestConnection": "Host=localhost;Username=postgres;Database=CardOverflow_{TestName};",
    "ServerConnection": "Host=localhost;Username=postgres;", // used to connect to the server and create a db
    "ElasticSearchUri": "http://localhost:9200"
  },
  "Serilog": {
    "WriteTo": [
      {
        "Name": "Trace"
      }
    ]
  }
}
